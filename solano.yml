:ruby_version: 'ruby-2.1.5'

python:
  python_version: 2.7
  pip_requirements_file: requirements.txt

#hooks:
#  package_setup: "pip install -U pip && pip install -r requirements.txt"

tests:
  - PYTHONPATH=. python test/myunit_app_test.py

